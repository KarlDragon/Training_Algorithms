#include <iostream>
#include <fstream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    
    ifstream INP_file("BAI3.INP");
	ofstream OUT_file("BAI3.OUT");

	INP_file.close();
	OUT_file.close();
    
    return 0;
}
 
 
